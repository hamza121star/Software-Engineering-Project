# Add a declarative step here for populating the DB with movies.

#Given /logged as an instructor/ do |courses_table|
 # courses_table.hashes.each do |course|
 #   Course.create!(course)
 # end
#end

# See the input course
#Then /I should see "(.*)"/
